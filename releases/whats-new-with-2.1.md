# What's New With 2.1

11/26/2022

## New Features

* Support returning HTML directly from `onRender()` (no `.cfm` file needed)

## Bugs

* Fix error when computed properties return no value
* Fix nested components only rendering the last template
* Fix struct values being replaced with empty strings in templates
* Support ColdBox apps in subdirectories
* Fix rendering errors with latest ColdBox version
* Fix nested components not rendering at all

