# What's New With 3.2

12/20/2023

## New Features

* Auto-populate data props when `onMount()` isn't defined (Michael Rigsby)
* Support `wire("MyComponent@module")` calls (Michael Rigsby)
* Add browser event dispatching
* Support `event.method()` calls in templates
